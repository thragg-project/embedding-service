apiVersion: v1
kind: Service
metadata:
  name: go-template
  namespace: go-template
  labels:
    app.kubernetes.io/name: go-template
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: go-template
  ports:
    - name: api
      port: 8000
      protocol: TCP
      targetPort: api
